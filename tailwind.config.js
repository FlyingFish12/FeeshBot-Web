const colors = require(tailwindcss/colors)

module.exports = {
  purge: [],
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {
      colors: {
        navbar: '#131316',
        navbartext: '#f9f7f3'
      }
    },
  },
  variants: {
    extend: {},
  },
  plugins: [],
}